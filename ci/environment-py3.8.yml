name: test_env_climate_index_collection
channels:
  - conda-forge
dependencies:
  - python=3.8
  ############## These will have to be adjusted to your specific project
  - dask
  - numpy
  - netcdf4
  - scipy
  - xarray
  ##############
  - pytest
  - pip:
      - codecov
      - pytest-cov
      - coverage[toml]
